# overrides if .env variables not detected, must be valid

ROBLOX_API_KEY = "" # api key for specific responses
ROBLO_SECURITY = "" # for bot account, needs high enough permissions, dont share with anyone other than yourself

# non-overrides

# api keys to access Relix with
RELIX_API_KEYS = [
    "", # insert keys here
    ""
]
DISCORD_WEBHOOK = ""  # default webhook